curl -f --connect-timeout 3 localhost:1521 2> /tmp/oracle-error
echo $?
echo 52 is the service is up

curl -f --connect-timeout 3 localhost:5432 2> /tmp/postgresql-error
echo $?
cat /tmp/postgresql-error
echo $?

curl -v telnet://localhost:1521
curl -vLI telnet://www.google.com:80/ <<< "GET /"
curl -vLI telnet://localhost:1521/ <<< "GET /"
