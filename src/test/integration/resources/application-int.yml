spring:
  main:
    web-application-type: servlet
  graphql:
    path: /graphql
    schema:
      locations: classpath:graphql/

custom:
  project:
    jwt:
      # 32+ char test key for HMAC
      key: 0123456789abcdef0123456789abcdef
